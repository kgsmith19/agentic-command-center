# specs/active

In-progress specs live here as `SPEC-NNNN-<kebab-title>.md` (template: `templates/SPEC.md`). `git mv` a spec to `specs/done/` when it passes its Definition of Done. In flight now: SPEC-0005 (headless launch surface + keystroke-stack retirement) — PR-1 and PR-2 shipped; it moves to done after Kyle's manual Windows check (its §12).
