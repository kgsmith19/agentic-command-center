# specs/active

In-progress specs live here as `SPEC-NNNN-<kebab-title>.md` (template: `templates/SPEC.md`). Empty today — this repo has no spec-driven slice in flight; work so far has been direct fixes against the already-shipped system. `git mv` a spec to `specs/done/` when it passes its Definition of Done.
