# specs/active

In-progress specs live here as `SPEC-NNNN-<kebab-title>.md` (template: `templates/SPEC.md`). `git mv` a spec to `specs/done/` when it passes its Definition of Done. In flight now: SPEC-0002/0003/0004 (web-GUI tab migration, awaiting their DoD sweep) and SPEC-0005 (headless launch surface + keystroke-stack retirement).
