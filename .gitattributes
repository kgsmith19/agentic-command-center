# shim/claude is a POSIX shell script (Git Bash) — CRLF line endings break
# `#!/bin/sh` on some interpreters, so pin it to LF regardless of the
# checking-out machine's core.autocrlf.
shim/claude text eol=lf
